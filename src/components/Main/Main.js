import React from "react";

import Convert from "./../Convert/Convert";
import Input from "./../Input/Input";

const Main = (props) => {
  return (
    <div className="Main">
      <Convert />
      <Input />
    </div>
  );
};

export default Main;
